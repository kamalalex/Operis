/**
 * Central Modules Entrypoint
 * 
 * To register a new business module:
 * 1. Create your module folder under `/src/modules/<module-name>`
 * 2. In your module, define a module configuration file (e.g., `src/modules/<module-name>/index.ts`)
 *    where you call `moduleRegistry.register(...)`
 * 3. Import that configuration file here to boot the module.
 * 
 * Example:
 * import "./task"; // Registers the task module
 */

// Import active business modules:
// import "./task"; // Commente pour l'instant (MVP)
